<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/countryRow"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="?attr/colorOnSecondary"
    app:cardCornerRadius="@dimen/corner_radius"
    app:cardElevation="@dimen/item_elevation"
    app:cardMaxElevation="@dimen/item_elevation"
    app:cardPreventCornerOverlap="true"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/side_margin"
        android:layout_marginEnd="@dimen/side_margin"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/history_icon"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/history_icon_height"
            android:layout_gravity="center_vertical"
            android:contentDescription="@string/recently_viewed"
            android:src="@drawable/ic_baseline_access_time_24"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/countryName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="@dimen/margin_text_row"
            android:textColor="?attr/colorSecondary"
            android:textSize="@dimen/country_name_text_row" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
